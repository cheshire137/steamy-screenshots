{
  "name": "SteamyScreenshots",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "git://github.com/moneypenny/steamy-screenshots.git"
  },
  "dependencies": {
    "lunr-index-build": "0.0.1"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "postinstall": "if [ -f steam-apps.json ]; then node_modules/.bin/lunr-generator -r appid -f name < steam-apps.json > steam-apps-index.json; fi"
  },
  "devDependencies": {}
}
